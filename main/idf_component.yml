dependencies:
  idf:
    version: '>=4.4.0'
  esp_lcd_ek79007:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=5.3 && target in [esp32p4]"
  espressif/esp_hosted:
    version: "^2.0.0"
    rules:
      - if: "target in [esp32p4]"
  espressif/esp_wifi_remote:
    version: "*"
    rules:
      - if: "target in [esp32p4]"
